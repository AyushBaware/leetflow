# LeetFlow

Chrome extension that reviews and pushes your accepted LeetCode submissions to a GitHub repository — with a folder picker and an explicit push step, instead of auto-committing blindly.

🚧 Under active development.